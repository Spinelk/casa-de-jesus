<div class="voluntario md:bg-cover bg-none">
    <div class="flex w-full h-full" id="formDiv">
        <div class="w-full md:w-1/2 bg-cover md:block hidden"></div>
        <div class="w-full md:w-1/2 m-0 p-0 flex-grow">
            <div class="voluntario-form p-16">
                <form class="max-w-lg mr-auto">
                    <h2 class="text-3xl font-bold mb-5">
                        ¡Sé voluntario con nosotros!
                    </h2>
                    <div class="mb-5">
                        <label
                            for="email"
                            class="block mb-2 text-sm font-medium text-gray-900 dark:text-white"
                            >Correo:</label
                        >
                        <input
                            type="email"
                            id="email"
                            class="input-style"
                            placeholder="Correo electrónico"
                            required
                        />
                    </div>
                    <div class="mb-5">
                        <label
                            for="tel"
                            class="block mb-2 text-sm font-medium text-gray-900 dark:text-white"
                            >Contacto telefónico:</label
                        >
                        <input
                            type="tel"
                            id="tel"
                            class="input-style"
                            placeholder="(+569...)"
                            required
                        />
                    </div>

                    <button type="submit" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Enviar</button>
                </form>
            </div>
        </div>
    </div>
</div>

<style>
    /* Estilos generales */
    .voluntario {
        background-image: url(voluntario.jpg);
        background-position: center center;
        background-size: cover;
        background-repeat: no-repeat;
        position: relative;
        overflow: hidden;
        height: 24rem; /* 96 * 0.25rem = 24rem */
    }

    .voluntario-form {
        background-image: url(fondo01.png);
        background-position: center center;
        background-size: cover;
        text-align: left;
    }

    .w-1/2.bg-cover {
        display: block; /* Se muestra por defecto */
    }

/* Estilos para pantallas pequeñas */
@media (max-width: 600px) {
    .voluntario {
        background-image: none; /* Oculta la imagen de fondo */
    }

    .w-1/2.bg-cover {
        display: none !important; /* Oculta el div vacío */
    }

    .w-1/2.m-0.p-0.flex-grow {
        width: 100%; /* Hace que el div del formulario ocupe todo el espacio */
    }
}
</style>
